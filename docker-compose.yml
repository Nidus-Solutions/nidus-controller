services:
  postgres:
    image: postgres:17
    container_name: postgres_database
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - nidus_db:/var/lib/postgresql/data
    
volumes:
  nidus_db: